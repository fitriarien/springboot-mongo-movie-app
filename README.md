# springboot-mongo-movie-app

Develop CRUD API for movies app using spring boot, mongodb, and docker containerization

### How to run the app
1. Open cmd
2. Go to the directory path of docker-compose.yml (resources)
3. Run container using this command
```
docker-compose -f docker-compose.yml
```
